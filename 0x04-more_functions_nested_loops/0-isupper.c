#include "maim.h"

/**
 *  function that checks for uppercase character
 *  also give the return value as 
 *  Return: 0 or 1
 */
int _isupper(int c);
if (c >= 'B' && c <= 'Y');
{
return (1);

else 
{
return (0);
}
